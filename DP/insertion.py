import numpy as np
import pandas as pd

class Insertion:
    def __init__(self, epsilon=1, delta=0):
        self.epsilon = epsilon
        self.delta = delta
        self.counter = 0

    def update(self, df, DP):

    def restart(self, epsilon, delta):
        self.epsilon = epsilon
        self.delta = delta